{
  "rules": {
    "terminology": {
      "defaultTerms": true,
      "exclude": [
        "repo\\b",
        "bug[- ]fix(es)?",
        "build system(s)?",
        "readme(s)?",
        "to-?do(s)?(?=[ ,.])"
      ]
    }
  }
}
